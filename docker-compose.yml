version: "3.2"
services:
  web:
    build:
      context: './php/'
      args:
        PHP_VERSION: ${PHP_VERSION}
    networks:
      - backend
    ports:
      - "6001:6001"
    volumes:
      - ${PROJECT_ROOT}/:/var/www/html/
    container_name: php_shopify
networks:
  backend:
